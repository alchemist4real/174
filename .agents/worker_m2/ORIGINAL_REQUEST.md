## 2026-07-13T13:32:26Z
You are teamwork_preview_worker. Your working directory is: d:\DOWNLOAD\MR-CAPSULES-main\.agents\worker_m2
Your task is to refactor `admin.html` and its associated JavaScript files (`admin.js` and `admin-workflow.js`) in accordance with the design guidelines and the blueprint generated by the explorer.

You must read:
- The explorer's analysis at `d:\DOWNLOAD\MR-CAPSULES-main\.agents\explorer_m2_1\analysis.md`
- The project styling guidelines and contracts in `PROJECT.md`
- The user request in `d:\DOWNLOAD\MR-CAPSULES-main\.agents\orchestrator\ORIGINAL_REQUEST.md`

Specifically, you MUST ensure:
1. `admin.html` is switched to load `/global-styles.css` and NOT `admin.css` or `new_styles.css`.
2. Remove `<div class="noise"></div>` and `<div class="scanlines"></div>` from `admin.html`.
3. Cards and containers use `border: 1.5px solid var(--text-main)` and `border-radius: 8px`. In `global-styles.css` or `admin.html` inline styles, ensure stats cards and wrappers use `border-radius: var(--radius-card)` and `border: var(--border-main)`.
4. The `.kanban-card` border must be `1.5px solid var(--text-main)`.
5. Buttons (e.g. `.btn-unified` primary buttons) must use `99px` border-radius (`border-radius: var(--radius-pill)`).
6. Use `Times New Roman` for headers (prominent headers, h2 in toolbars) and `Courier New` for data/code.
7. Refactor the modals (`#createTaskModal`, `#promptModal`, `#editorModal`, `#contextModal`, `#divisionPickerModal`) to follow the `.settings-overlay` + `.settings-box` + `.settings-header` + `.settings-body` nested DOM architecture.
8. Update the modal toggle JavaScript in `admin.js` and `admin-workflow.js` to toggle the `.active` class (instead of adding/removing `.hidden`), aligning with the transition styles in `global-styles.css`.
9. Ensure duplicate style attributes (e.g. on `h2` elements) are resolved.
10. Ensure all `<svg>` elements have `aria-label` or `aria-hidden="true"`.
11. Ensure status rows have `role="status"` or `aria-live="polite"`.
12. Remove the 🗑️ emoji from `admin.html` line 120, and ensure no dynamic emojis are injected (such as in `admin.js` Guest Cleanup button).
13. Run `npm run build` or `node build.js` to compile the catalog database if any configuration or files changed.

MANDATORY INTEGRITY WARNING:
DO NOT CHEAT. All implementations must be genuine. DO NOT hardcode test results, create dummy/facade implementations, or circumvent the intended task. A Forensic Auditor will independently verify your work. Integrity violations WILL be detected and your work WILL be rejected.

After refactoring, verify that the page renders correctly, there are no layout or visual bugs, buttons work, and theme toggling functions properly. Write your handoff and return.

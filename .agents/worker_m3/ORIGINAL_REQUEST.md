## 2026-07-13T13:42:47Z
You are teamwork_preview_worker. Your working directory is: d:\DOWNLOAD\MR-CAPSULES-main\.agents\worker_m3
Your task is to refactor `live.html`, `live.js`, and the toast notification setup in `admin.js` in accordance with the design guidelines and the blueprint generated by the explorer.

You must read:
- The explorer's analysis at `d:\DOWNLOAD\MR-CAPSULES-main\.agents\explorer_m3\analysis.md`
- The project styling guidelines and contracts in `PROJECT.md`
- The user request in `d:\DOWNLOAD\MR-CAPSULES-main\.agents\orchestrator\ORIGINAL_REQUEST.md`

Specifically, you MUST ensure:
1. `live.html` loads `global-styles.css` and NOT `admin.css` or `new_styles.css`.
2. Remove `<div class="noise"></div>` and `<div class="scanlines"></div>` from `live.html` if they exist.
3. Switch the script tag in `live.html` from loading `admin-workflow.js` to loading `live.js`. (Check: `<script src="/live.js?v=1.2"></script>`).
4. Cards and containers use `border: 1.5px solid var(--text-main)` and `border-radius: 8px` (using `--border-main` and `--radius-card`). Apply this in `live.html` and `live.js` for kanban columns, review editor header, stats blocks, WhatsApp settings container, and division cards.
5. Primary buttons use `99px` border-radius (`border-radius: var(--radius-pill)` or class `.btn-unified.primary`).
6. Remap modal overlays in `live.html` (`createTaskModal`, `promptModal`, `editorModal`, `contextModal`, `divisionPickerModal`) to follow the `.settings-overlay` + `.settings-box` + `.settings-header` + `.settings-body` nested DOM structure.
7. Update the modal toggle logic in `live.js` (and any related files) to toggle the `.active` class (instead of `.hidden`).
8. Fix typography: headers use `Times New Roman` (`var(--font-sans)`) and data/code displays use `Courier New` (`var(--font-mono)`). Resolve duplicate style attributes on `h2` elements.
9. Accessibility: Add `role="status"` and `aria-live="polite"` to `#authMessage` in `live.html` and dynamic toasts in `admin.js`. Resolve duplicate `role="status"` and `aria-live="polite"` attributes on `#statusText` in `live.html`. Add `aria-hidden="true"` to dynamic SVGs in `live.js`.
10. Remove any legacy emojis if present.
11. Run `npm run build` or `node build.js` to compile the catalog database if any configuration or files changed.
